
{
  "name": "chief-blog",
  "version": "1.0.0",
  "description": "A small news/blog site with Express, MySQL, and static HTML pages.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "check": "node --check server.js && node --check controllers/authController.js && node --check controllers/postController.js && node --check routes/auth.js && node --check routes/posts.js"
  },
  "dependencies": {
    "bcrypt": "^6.0.0",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.21.0",
    "jsonwebtoken": "^9.0.2",
    "mysql2": "^3.11.0"
  },
  "devDependencies": {
    "nodemon": "^3.1.10"
  }
}
